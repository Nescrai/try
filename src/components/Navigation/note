<!-- import { FaStackExchange, FaWindowClose } from "react-icons/fa";
import Dropdown from "./Dropdown";
import { useState } from "react";
import "./NavBar.css";

function NavBar({ onRouteForm }) {
  const [open, setOpen] = useState(false);

  const handleMenu = () => {
    setOpen(!open);
  };

  return (
    <div className="bar">
      <header>
        <div className="navbar">
          <div className="logo">
            <a href="try">Let`s try</a>
          </div>
          <ul className="cell">
            <li>
              <a href="home">Home</a>
            </li>
            <li>
              <a href="about">About</a>
            </li>
            <li>
              <a href="services">Services</a>
            </li>
            <li>
              <a href="contact">Contact</a>
            </li>
          </ul>
          <button className="action_btn">Get Started</button>
          <li>
            <a onClick={() => onRouteForm('signin')} href="signin">Signin</a>
          </li>
          <div className="stack">
            <FaStackExchange onClick={handleMenu} />
          </div>
        </div>
        <div>{open && <Dropdown />}</div>
      </header>
    </div>
  );
}

export default NavBar; -->